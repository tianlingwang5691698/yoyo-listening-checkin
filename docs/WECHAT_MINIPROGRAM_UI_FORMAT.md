# 微信小程序页面格式

## 页面边界

- 页面按手机首屏设计，不按桌面网页排版。
- 主内容优先纵向排列，不做横向工具栏堆叠。
- 同一屏只保留一个主要操作区。

## 按钮层级

- 主操作使用整行按钮。
- 次级切换使用分段按钮。
- 文本内容可点时，整句作为点击区，不再叠小按钮。

## 阅读页

- 原文按句子拆分，每句都可点击。
- 提交后轻触句子只翻译当前句，不生成全文翻译；翻译中给行内弱提示，同句再次轻触收起。
- 同一页面内已翻译句子必须复用内存缓存，不重复请求云端。
- 不设置句子发音模式。
- 学习包只保留生词、短语、句型三类卡。
