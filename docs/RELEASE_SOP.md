# 提审前上线 SOP

## 目标

把当前工程收口到“可提审版本”，不把实验代码、调试残留和未验证流程带进上传包。

## 固定顺序

1. 整理工作区
2. 确认正式配置
3. 部署云函数
4. 真机自测
5. 生成真机稳定基线提交
6. 去掉开发态调试提示
7. 真机复测
8. 上传代码
9. 生成体验版
10. 准备提审材料
11. 发起提审

## 体验版前置门槛

- 首页必须是 `syncMode = cloud`
- 首页不能再出现 `cloud-error`
- `yoyo` 在正式环境中可稳定执行
- `Unlock1` 前台只出现 `>= 60 秒` 的音频
- `unlock1AudioTrainingPool` 已存在 `eligible`
- `Songs` 前台按句级文本同步工作，不再要求整批逐词稳定
- 当前工作区已整理成可复测的稳定基线

## 每步检查项

### 1. 整理工作区

- 未提交改动按“提审相关 / 在研实验”分开
- 不把临时脚本、测试文案、试验数据混入提审版本

### 2. 确认正式配置

- `data/app-config.js` 为正式配置源
- AppID、环境 ID、桶名、访问域名一致
- `subscriptionTemplateIds` 若要提审订阅功能，需填正式模板 ID

### 3. 部署云函数

- `yoyo` 云函数依赖完整
- 上传部署到正式环境
- 云函数日志无明显报错
- 若本次没有改 `Unlock1` 训练池扫描逻辑，不要求重复部署 `unlock1-preprocess`

### 4. 真机自测

- 按 [docs/REAL_DEVICE_TEST.md](/Users/wangtianlong/工作/工作流/微信小程序/佑佑听力打卡/docs/REAL_DEVICE_TEST.md) 执行
- 所有关键链路通过
- 当前前台已不再允许本地 fallback，若看到云错误必须先修云端，再继续后续步骤

### 5. 真机稳定基线提交

- 形成一版“真机稳定基线”
- 该版本允许保留开发态调试提示，仅用于内部确认

### 6. 去掉开发态调试提示

- 在 [data/app-config.js](/Users/wangtianlong/工作/工作流/微信小程序/佑佑听力打卡/data/app-config.js) 中切换：
  - `releaseStage = 'review'`
  - `showCloudDebug = false`
- 页面不再显示给审核员看的调试信息
- 保留代码里的判断逻辑，但提审包不可见

### 7. 真机复测

- 用提审视角复测完整路径
- 页面无明显占位、无调试文案、无假数据感知

### 8. 上传代码

- 上传当前可提审版本
- 记录版本号与说明

### 9. 生成体验版

- 用管理员微信和家庭成员微信复测
- 确认体验版与真机调试表现一致
- 若上传后首页仍报云错误，不要继续提审，先回到步骤 3 和步骤 4 处理云端问题

### 10. 准备提审材料

- 功能简介
- 审核路径
- 测试账号说明
- 隐私政策说明
- 核心页面截图
- `Songs` 若接入提审，只能描述为“音频 + 句级文本同步”，不要报逐词稳定能力

### 11. 发起提审

- 按真实能力填写，不多报权限
- 如歌曲目录为空，需说明该模块的展示逻辑

## 可提审最小能力

- 音频任务稳定播放
- 打卡与成长记录可用
- 家庭共享可用
- 家长日报可读
- 页面无明显调试残留
- 云端异常不会造成白屏
