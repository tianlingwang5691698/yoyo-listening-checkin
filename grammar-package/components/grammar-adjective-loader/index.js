const { buildAdjectiveCourse } = require('../../domain/grammar-classroom/adjective-courses');
Component({properties:{topic:{type:String,value:''},language:{type:String,value:'zh-CN'}},observers:{'topic, language':function(t,l){if(this._ready)this.load(t,l)}},lifetimes:{ready(){this._ready=true;this.load(this.data.topic,this.data.language)}},methods:{load(topic,language){if(topic!=='adjective')return;try{this.triggerEvent('loaded',{topic,bundle:buildAdjectiveCourse(language==='en')})}catch(error){this.triggerEvent('loaderror',{topic,message:error&&error.message||'unknown'})}}}});
