const courses=require('../../domain/grammar-classroom/punctuation-courses');Component({properties:{topic:{type:String,value:''},language:{type:String,value:'zh-CN'}},observers:{'topic, language':function(t,l){if(this._ready)this.load(t,l)}},lifetimes:{ready(){this._ready=true;this.load(this.data.topic,this.data.language)}},methods:{load(topic,language){if(topic!=='punctuation')return;const key=`${topic}:${language}`;if(this._loadedKey===key)return;try{const bundle=courses.buildPunctuationCourse(language==='en');this._loadedKey=key;this.triggerEvent('loaded',{topic,bundle})}catch(error){this.triggerEvent('loaderror',{topic,message:error&&error.message||'unknown'})}}}});
